# Handoff

## Текущий handoff

**Кому:** Staff Engineer / EM (следующая фаза — реализация).
**Что передаётся:** утверждённый дизайн модульного механизма.

- Решение архитектуры: `.10x/decisions/architect/module-system.md` (компоненты, манифест, модель данных, API-контракты, failure modes, порядок миграции, acceptance contract).
- Визуализация: `docs/architecture.md` (C4 Context + Container, Mermaid).
- Индексы/принципы: `.10x/decisions/architect/_index.md`.
- Согласованные границы: один процесс + одна SQLite-БД; простые модули = только манифест; хост — единственная точка монтажа с обязательным `core.can`; платформа (`/api/auth`, `/api/admin`) остаётся платформенной.
- Объём фазы реализации: этапы 1–7 из решения; Docker/README/бэкапы — вне текущего этапа.
- Ключевой продукт-контракт: владелец добавляет простой модуль одним `manifest.json` без правки кода оболочки; права из `/me` реально режут UI и API; сессии инвалидируются при сбросе пароля/снятии админа.

## История handoff

(пусто)
